{
  "name": "oo-slimopslagsysteem",
  "version": "1.0.0",
  "description": "Slim Opslagsysteem - PWA Development",
  "scripts": {
    "dev": "node scripts/startup-banner.js && concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd project/backend && node server.js",
    "dev:frontend": "cd project/frontend && npm run dev",
    "start": "npm run dev",
    "build": "npm install --prefix project/backend && npm install --prefix project/chatbot && npm install --prefix project/frontend",
    "postinstall": "npm install --prefix project/backend && npm install --prefix project/chatbot && npm install --prefix project/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
